# Core scientific computing dependencies
numpy<2.0.0  # For compatibility with PyTorch's current build
scipy==1.11.4
pandas==2.1.4
scikit-learn==1.3.2

# Required by transformers
regex==2023.10.3
Pillow==10.1.0  # Required for image processing
safetensors>=0.4.1  # For secure model loading

# PyTorch and CUDA dependencies
--index-url https://download.pytorch.org/whl/cu121
torch==2.1.1+cu121  # Fixed version for compatibility
torchvision==0.16.1+cu121  # Compatible with torch 2.1.1
torchaudio==2.1.1+cu121  # Compatible with torch 2.1.1

# Hugging Face Transformers and datasets
transformers>=4.36.0  # Using newer version for better security
datasets
evaluate

# Utilities
tqdm>=4.65.0
psutil>=5.9.0
GPUtil>=1.4.0  # Note: package name is GPUtil (uppercase) but imported as gputil 